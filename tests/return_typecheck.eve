module hello;

main := fn() {
    let z = 0;
    dbg succ(z);
    dbg succ(succ(z));
    dbg succ(succ(succ(z)));
    dbg gt(69, 420);
}

succ := fn(x: s32) -> s32 {
    return x + 1;
}

gt := fn(a: s32, b: s32) -> bool {
    return 1;
}
