module hello;

type str = []u8;
type cstr = *u8;

main :: fn() {
    let xs: [_]u8 = {69, 420};
    let slc: str = xs[..];
    foo(slc);
    let x: u8 = 69;
    let c: cstr = &x;
}

foo :: fn(xs: str) {
    // nothing for now
}
